<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLE permissions (scan only; the library never connects/bonds, so no BLUETOOTH_CONNECT).
         BLUETOOTH_SCAN also satisfies the connectedDevice foreground-service type. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <!-- For Android 11 and below -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Location permissions (required for BLE scanning on Android < 12) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- ACCESS_BACKGROUND_LOCATION is intentionally not declared by the library. A host that opts in
         to restartOnBoot must declare and request it in the application. -->

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- POST_NOTIFICATIONS is intentionally not declared. The scan foreground service posts its
         required ongoing notification either way; without the permission Android 13+ just keeps it
         out of the notification drawer (it stays visible in the Task Manager). A host that wants it
         shown declares and requests the permission itself. -->

    <!-- Background scanning -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application>
        <service
            android:name="com.nanobeaconnetwork.internal.service.BleScanService"
            android:foregroundServiceType="location|connectedDevice"
            android:exported="false" />

        <!-- Resumes LIBRARY_SCAN scanning after a reboot (gated by NbnConfig.restartOnBoot +
             whether scanning was active). exported=true is required to receive the system
             BOOT_COMPLETED broadcast. -->
        <receiver
            android:name="com.nanobeaconnetwork.internal.service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
