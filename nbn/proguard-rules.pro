# ProGuard/R8 rules applied when building the library itself with minification enabled.
# The library ships unminified (isMinifyEnabled = false), so this file is normally empty.
# Rules that consumers need are in consumer-rules.pro (applied to the host app instead).
