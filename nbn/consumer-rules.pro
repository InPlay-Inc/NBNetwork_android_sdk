# Library consumer ProGuard rules — automatically applied to host app
# Public calls are retained by app references; reflected Gson fields use @SerializedName.
# Broad keep rules would unnecessarily reduce host-app shrinking and optimization.
